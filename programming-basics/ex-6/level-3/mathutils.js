const mathutils = {
    sin: (x) => Math.sin(x),
    cos: (x) => Math.cos(x),
    tan: (x) => Math.tan(x),
    // You can add more mathematical functions as needed
  };
  
  module.exports = mathutils;
  